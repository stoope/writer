{
  "name": "writer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "electron.cjs",
  "scripts": {
    "start": "NODE_ENV=development concurrently \"npm run frontend:start\" \"npm run electron:start\"",
    "frontend:start": "vite --port 5000 --host localhost",
    "frontend:build": "vite build",
    "frontend:preview": "vite preview",
    "frontend:check": "svelte-check --tsconfig ./tsconfig.json",
    "electron:start": "electron .",
    "electron:build": "electron-builder",
    "build": "frontend:build && electron:build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.0.2",
    "@tsconfig/svelte": "^3.0.0",
    "@types/debounce": "^1.2.1",
    "concurrently": "^7.6.0",
    "electron": "^23.1.0",
    "electron-builder": "^22.10.3",
    "svelte": "^3.55.1",
    "svelte-check": "^2.10.3",
    "tslib": "^2.5.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  },
  "build": {
    "files": [
      "src/**/*"
    ]
  },
  "volta": {
    "node": "18.14.1"
  },
  "dependencies": {
    "@electron/remote": "^2.0.9",
    "@fontsource/courier-prime": "^4.5.9",
    "@fontsource/fira-code": "^4.5.13",
    "debounce": "^1.2.1",
    "remove": "^0.1.5"
  }
}
